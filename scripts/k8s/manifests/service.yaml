apiVersion: v1
kind: Service
metadata:
  name: fastclaw
  labels:
    app: fastclaw
spec:
  type: ClusterIP
  selector:
    app: fastclaw
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
