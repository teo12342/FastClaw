---
summary: "CLI reference for `fastclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `fastclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
fastclaw uninstall
fastclaw uninstall --all --yes
fastclaw uninstall --dry-run
```
